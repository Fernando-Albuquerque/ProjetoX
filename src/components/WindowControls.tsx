import React from 'react';

const WindowControls: React.FC = () => {
    return (
        <div className="window-controls">
            <div className="drag-region">
                {/* Transparent drag region */}
            </div>
        </div>
    );
};

export default WindowControls;
